// ========================================================================
// SCUI.DateView Tests
// ========================================================================

/* Test SCUI.DateView */

/*var pane = SC.ControlTestPane.design()
  .add("basic,past", SCUI.DateView, {
    layout: {width: 50, height: 50},
    date: 1,
    timing: SCUI.PAST
  })
  .add("basic,present", SCUI.DateView, {
    layout: {width: 50, height: 50},
    date: 1,
    timing: SCUI.PRESENT
  })
  .add("basic,present,selected", SCUI.DateView, {
    layout: {width: 50, height: 50},
    date: 1,
    isSelected: YES,
    timing: SCUI.PRESENT
  })
  .add("basic,today", SCUI.DateView, {
    layout: {width: 50, height: 50},
    date: 1,
    timing: SCUI.TODAY
  })
  .add("basic,today", SCUI.DateView, {
    layout: {width: 50, height: 50},
    date: 1,
    isSelected: YES,
    timing: SCUI.TODAY
  })
  .add("basic,future", SCUI.DateView, {
    layout: {width: 50, height: 50},
    date: 1,
    timing: SCUI.FUTURE
  });
  
  
pane.show(); // add a test to show the test pane
window.pane = pane ;

// ..........................................................
// BASIC TESTS
// 
module("Basic Tests", pane.standardSetup());*/

